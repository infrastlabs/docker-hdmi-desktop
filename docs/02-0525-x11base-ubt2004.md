

### 0525|ap34-n3410-intel `x11base:ubt20: --no-install-recommends`

- xorg 14.6 MB; xserver-xorg 4762 kB; pulseaudio 7255 kB

```bash
root@ap34:/# apt install --no-install-recommends xorg
  The following NEW packages will be installed:
    bsdmainutils groff-base keyboard-configuration libfontenc1 liblocale-gettext-perl libpipeline1 libuchardet0 libxfont2 libxv1 libxxf86dga1
    man-db python-apt-common python3-apport python3-apt python3-blinker python3-certifi python3-cffi-backend python3-chardet
    python3-cryptography python3-dbus python3-distro python3-entrypoints python3-httplib2 python3-idna python3-jwt python3-keyring
    python3-launchpadlib python3-lazr.restfulclient python3-lazr.uri python3-oauthlib python3-pkg-resources python3-problem-report
    python3-requests python3-requests-unixsocket python3-secretstorage python3-simplejson python3-six python3-urllib3 python3-wadllib udev
    x11-apps x11-session-utils x11-utils xauth xfonts-base xfonts-encodings xfonts-utils xinit xinput xorg xorg-docs-core xserver-common
    xserver-xorg xserver-xorg-core xserver-xorg-input-all xserver-xorg-input-libinput
  0 upgraded, 56 newly installed, 0 to remove and 48 not upgraded.
  Need to get 14.6 MB of archives.



root@ap34:/# apt install --no-install-recommends xserver-xorg
  The following NEW packages will be installed:
    keyboard-configuration libfontenc1 liblocale-gettext-perl libxfont2 python-apt-common python3-apport python3-apt python3-blinker
    python3-certifi python3-cffi-backend python3-chardet python3-cryptography python3-dbus python3-distro python3-entrypoints python3-httplib2
    python3-idna python3-jwt python3-keyring python3-launchpadlib python3-lazr.restfulclient python3-lazr.uri python3-oauthlib
    python3-pkg-resources python3-problem-report python3-requests python3-requests-unixsocket python3-secretstorage python3-simplejson
    python3-six python3-urllib3 python3-wadllib udev xserver-common xserver-xorg xserver-xorg-core xserver-xorg-input-all
    xserver-xorg-input-libinput
  0 upgraded, 38 newly installed, 0 to remove and 48 not upgraded.
  Need to get 4762 kB of archives.


root@ap34:/# apt install --no-install-recommends pulseaudio  
  The following NEW packages will be installed:
    iso-codes libasound2-plugins libgomp1 libgstreamer-plugins-base1.0-0 libgstreamer1.0-0 libjack-jackd2-0 liborc-0.4-0 libpulsedsp
    libsamplerate0 libsnapd-glib1 libsoxr0 libspeexdsp1 libwebrtc-audio-processing1 pulseaudio pulseaudio-utils
  0 upgraded, 15 newly installed, 0 to remove and 48 not upgraded.
  Need to get 7255 kB of archives.
  ..
  Configuration file '/etc/pulse/default.pa'
  ==> File on system created by you or by a script.
  ==> File also in package provided by package maintainer.
    What would you like to do about it ?  Your options are:
      Y or I  : install the package maintainer\'s version
      N or O  : keep your currently-installed version
    D     : show the differences between the versions
    Z     : start a shell to examine the situation
  The default action is to keep your current version.
  *** default.pa (Y/I/N/O/D/Z) [default=N] ? Y
```


- test `/dev/input/$input` `xserver-xorg-input-evdev`

```bash
headless @ ap34 in ~ |13:56:30  
  $ export DISPLAY=:0.0
  $ st
  headless @ ap34 in ~ |13:59:40  
  $ startxfce4 #进到desk,all正常显示; mouse/key: TODO.input@/entrypoint.sh
  $ startfluxbox 

# input.sh @entrypoint.sh
  # add input devices and their events to X11 configuration
  mkdir -p /etc/X11/xorg.conf.d/
  conf=/etc/X11/xorg.conf.d/10-input.conf
  rm -f $conf
  # create new input device file
  cat > $conf <<_EOF_
Section "ServerFlags"
     Option "AutoAddDevices" "False"
EndSection
_EOF_

cd /dev/input
for input in event*
do
cat >> $conf <<_EOF_
    Section "InputDevice"
    Identifier "$input"
    Option "Device" "/dev/input/$input"
    Option "AutoServerLayout" "true"
    Driver "evdev"
EndSection
_EOF_
done

# apt install xserver-xorg-input-evdev
root@ap34:/# Xorg :0
  (EE) libinput bug: udev device never initialized (/dev/input/event9)
  (EE) client bug: Invalid path /dev/input/event9
  (EE) libinput bug: udev device never initialized (/dev/input/event0)
  (EE) client bug: Invalid path /dev/input/event0
  root@ap34:/# dpkg -l |grep xorg
  ii  xserver-xorg   1:7.7+19ubuntu14     amd64    X.Org X server
  ii  xserver-xorg-core  2:1.20.13-1ubuntu1~20.04.20   amd64    Xorg X server - core server
  ii  xserver-xorg-input-all  1:7.7+19ubuntu14     amd64    X.Org X server -- input driver metapackage
  ii  xserver-xorg-input-libinput  0.29.0-1    amd64    X.Org X server -- libinput input driver
  root@ap34:/# apt install xserver-xorg-input-evdev
  The following NEW packages will be installed:
    xserver-xorg-input-evdev
  0 upgraded, 1 newly installed, 0 to remove and 48 not upgraded.
  Need to get 31.9 kB of archives.
```

- pulse `pactl load-module module-alsa-sink device=plughw:0,3 #ok` pavucontro:` dummy> "HDA Intel PCH"`

```bash
####pulse
  $ pulseaudio
  $ pactl load-module module-alsa-sink device=plughw:0,3 #ok
  $ pavucontrol #view: dummy> "HDA Intel PCH"
  # play
  $ sudo apt install  lame sox libsox-fmt-mp3 
  $ play /_ext/3467.mp3

```

### 0526|genMachine-5700u-amdgpu

- amd-5700u尝试驱动 `xserver-xorg-video-amdgpu` `mesa-vulkan-drivers` `org:amdgpu-install_6.4.60401-1_all.deb`

```bash
# ref cust.md
#  2.genMachine--amd 不行(Xorg提示:识别不到屏幕); >> fk's img: 指定BUSID: 800X600分辨率;
root @ deb1013 in .../apps/docker-hdmi-desktop |13:53:35  |dev U:1 ✗| 
$ dpkg -l |grep amdgp
  ii  libdrm-amdgpu1:amd64  2.4.97-1  amd64   Userspace interface to amdgpu-specific kernel DRM services -- runtime
  ii  xserver-xorg-video-amdgpu  18.1.99+git20190207-1    amd64   X.Org X server -- AMDGPU display driver

# ref AI:
# apt install mesa-vulkan-drivers vulkan-utils
# root @ deb1013 in .../apps/docker-hdmi-desktop |13:57:22  |dev U:1 ✗| 
$ apt install mesa-vulkan-drivers vulkan-utils
  The following NEW packages will be installed:
    libvulkan1 libxcb-randr0 mesa-vulkan-drivers vulkan-tools vulkan-utils
  0 upgraded, 5 newly installed, 0 to remove and 123 not upgraded.
  Need to get 1766 kB of archives.

# org https://www.amd.com/en/support/download/linux-drivers.html#linux-for-radeon
# Run the following commands:
#  1.deb1013-host: deps不满足
#  2.ubt2004-ct  : deps不满足
  sudo apt update
  wget https://repo.radeon.com/amdgpu-install/6.4.1/ubuntu/jammy/amdgpu-install_6.4.60401-1_all.deb
  sudo apt install ./amdgpu-install_6.4.60401-1_all.deb
  sudo amdgpu-install -y --usecase=graphics,rocm
  sudo usermod -a -G render,video $LOGNAME
```

- genM_livecd:xbt20/24可高清显示; `xbt1710/deb1012:800x600`

```bash
# TODO:
  # 换ubt2204-ct

  # sandisk16G(白线)
  #  xbt1710.iso 1.22G; 内核?; 也是800x600;
  #  xbt2004.iso 1.83G; 5.4内核; 可高清显示(xxx 24寸:不能取得ips屏型号名);
  #  xubuntu-24.04.2-minimal-amd64.iso; 内核?; 可高清显示(xxx 24寸:不能取得ips屏型号名);


# deb10.13内核: 4.19 （显示800x600）
  root @ deb1013 in ~ |17:56:58  
  $ uname -a
    Linux deb1013 4.19.0-21-amd64 #1 SMP Debian 4.19.249-2 (2022-06-30) x86_64 GNU/Linux
```
